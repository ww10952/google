# 安装说明

## 直接拖拽安装

1. 打开 Chrome/Edge 浏览器
2. 访问 `chrome://extensions/` 或 `edge://extensions/`
3. 开启右上角的 **"开发者模式"** 开关
4. 将整个插件文件夹直接拖拽到浏览器窗口中
5. 点击 **"添加扩展程序"** 确认安装

## 使用快捷键

安装后，可以使用以下快捷键：
- `Ctrl+Shift+T` (Windows/Linux) 或 `Cmd+Shift+T` (Mac): 打开标签页管理器

## 文件清单

```
manifest.json        # 扩展配置文件
popup.html/css/js    # 主界面（900x600双栏布局）
background.js        # 后台服务
options.html/js      # 设置页面
icons/               # 图标文件
  ├── icon16.png
  ├── icon48.png
  └── icon128.png
```

## 界面布局说明

插件采用 **900x600** 的双栏布局：

**左侧面板 (300px)**:
- 分组列表（显示所有标签页分组）
- 创建/编辑/删除/折叠分组
- 筛选：全部标签 / 未分组标签

**右侧面板 (600px)**:
- 标签页列表（显示当前浏览器打开的所有标签页）
- 搜索功能
- 筛选：全部 / 活动 / 最近
- 多选标签页
- 快速操作（激活/关闭）

安装后即可在浏览器工具栏看到 Tab Manager Pro 图标，点击即可使用！
