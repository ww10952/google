// Configuration constants for Tab Manager Pro
const CONFIG = {
  // History settings
  HISTORY_MAX_RESULTS: 100,      // Initial load count
  HISTORY_LOAD_MORE: 50,         // Load more count per request
  HISTORY_DAYS: 30,              // History record days
  
  // Tab settings
  MAX_TABS_LIMIT: 100,           // Maximum tab limit
  
  // Storage settings
  STORAGE_QUOTA_WARNING: 4 * 1024 * 1024, // 4MB warning threshold
};
